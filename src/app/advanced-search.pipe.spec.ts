import { AdvancedSearchPipe } from './advanced-search.pipe';

describe('AdvancedSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new AdvancedSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
